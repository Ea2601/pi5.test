{
  "name": "pi5-supernode-backend",
  "version": "2.1.4",
  "description": "Pi5 Supernode Backend Services",
  "main": "dist/api-gateway/index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:gateway\" \"npm run dev:network\" \"npm run dev:vpn\" \"npm run dev:automation\"",
    "dev:gateway": "npm run dev --prefix ./api-gateway",
    "dev:network": "npm run dev --prefix ./network-service",
    "dev:vpn": "npm run dev --prefix ./vpn-service",
    "dev:automation": "npm run dev --prefix ./automation-service",
    "build": "npm run build:gateway && npm run build:network && npm run build:vpn && npm run build:automation",
    "build:gateway": "npm run build --prefix ./api-gateway",
    "build:network": "npm run build --prefix ./network-service",
    "build:vpn": "npm run build --prefix ./vpn-service",
    "build:automation": "npm run build --prefix ./automation-service",
    "start": "npm run start:gateway",
    "start:gateway": "npm start --prefix ./api-gateway",
    "migrate": "cd api-gateway && npm run migrate",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "keywords": ["network", "management", "vpn", "automation"],
  "author": "Pi5 Supernode Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}